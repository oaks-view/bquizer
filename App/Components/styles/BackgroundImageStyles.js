import { StyleSheet } from 'react-native';

export const styles = {
    container: {
        backgroundColor: '#FFF',
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        resizeMode: 'cover',
        zIndex: -4000,
        position: 'absolute'
    }
}